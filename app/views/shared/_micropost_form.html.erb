<%= form_with(model: @micropost, local: true) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
        <%= f.text_area :content, placeholder: "日記を書きます..." %>
    </div>
    <%= f.submit "登録", class: "btn btn-primary" %>
<% end %>